# ============================================
# Environment Variables Configuration
# ============================================
# Copy this file to .env and update with your actual values
# NEVER commit .env to version control!
# ============================================

# Database Configuration
# The app automatically detects if running on localhost or cPanel/hosted
# and uses appropriate defaults. You can override with environment variables.
#
# To force environment detection, set DEPLOYMENT_ENV:
#   - DEPLOYMENT_ENV=development (forces localhost defaults)
#   - DEPLOYMENT_ENV=production (forces cPanel/hosted defaults)
#
# Localhost/Development defaults:
#   DB_HOST=localhost
#   DB_USER=root
#   DB_PASSWORD=your_password_here
#   DB_NAME=modern_school
#
# cPanel/Hosted defaults (if not on localhost):
#   DB_HOST=localhost
#   DB_USER=projectl_school
#   DB_PASSWORD=Itskimathi007
#   DB_NAME=projectl_school
#
# Override defaults by setting these variables:
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_password_here
DB_NAME=modern_school

# Deployment Environment (optional - helps with auto-detection)
# Options: development, local, localhost, production, hosted, cpanel
DEPLOYMENT_ENV=development

# Flask Configuration
# Required: SECRET_KEY must be set to a secure random string
# Generate a secure key: python -c "import secrets; print(secrets.token_hex(32))"
# IMPORTANT: In production/hosted environments, this MUST be set to a secure value!
# The app will fail to start if SECRET_KEY is not set in production.
SECRET_KEY=your-secret-key-change-in-production
FLASK_ENV=development
FLASK_DEBUG=True

# Server Configuration
# Optional: Defaults shown below
HOST=0.0.0.0
PORT=5000

# Email Configuration (for sending notifications)
# Optional: Leave empty if not using email features
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=True
MAIL_USERNAME=your_email@gmail.com
MAIL_PASSWORD=your_app_password
MAIL_DEFAULT_SENDER=noreply@modernschool.com

# Support Contact Information
# Optional: Used for contact forms and support pages
SUPPORT_EMAIL=support@modernschool.com
SUPPORT_PHONE=+254 700 000 000
SCHOOL_NAME=Modern School


